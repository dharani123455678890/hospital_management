[build]
  publish = "frontend"

# Replace this host with the URL Render gives your backend.
[[redirects]]
  from = "/api/*"
  to = "https://REPLACE-WITH-YOUR-RENDER-SERVICE.onrender.com/:splat"
  status = 200
  force = true
